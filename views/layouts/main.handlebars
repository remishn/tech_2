<!DOCTYPE html>
<html lang="en">

<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Tech Blog</title>
  <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bulma@0.9.0/css/bulma.min.css">
  <link rel="stylesheet" href="/stylesheets/style.css">
  <script defer src="https://use.fontawesome.com/releases/v5.3.1/js/all.js"></script>
</head>

  <div class="wrapper">
    <header>
        <h1 class="has-text-primary px-10 py-10 has-text-weight-bold">Tech blog</h1>
        <nav class="navbar mt-2">
            {{#if loggedIn}}
            <a href="/" class="button is-primary">Home</a>
            <a href="/dashboard" class="button is-primary is-light">Dashboard</a>
            <a href="/dashboard/create/" class="button is-warning is-light">create post</a>
            <button id="logout" class="button is-danger">logout</button>
            {{else}}
            <a href="/" class="button is-primary">Home</a>
            <a href="/login" class="button is-primary is-light">login</a>
            {{/if}}
        </nav>
    </header>

    <main>
            {{{ body }}}
    </main>

  </div>

{{#if loggedIn}}
<script src="/javascript/logout.js"></script>
{{/if}}
</body>

</html>